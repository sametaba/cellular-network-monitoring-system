version: '3.8'

services:
  db:
    image: postgis/postgis:16-3.4
    container_name: cellular_db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=cellular_monitor
    ports:
      - "5432:5432"
    restart: unless-stopped